[project]
name = "pekka"
version = "0.1.0"
description = "search engine for netflix-like service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
